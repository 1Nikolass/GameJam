# Не та клавиша

**Игровой джем** · Тема: «Не та клавиша» · Дедлайн: 27 сентября 2026, 17:00

## Концепция

Игра, в которой главный герой управляется «неправильными» клавишами — привычные действия совершаются не теми кнопками, которых ожидает игрок. Механика построена вокруг путаницы и переобучения: прыжок там, где ожидался удар; атака там, где ожидалось движение. Сложность растёт по мере того, как схема управления меняется прямо во время игры.

## Команда

- Nikolass
- Danya
- Nil

---

## Быстрый старт

### Что нужно установить

| Инструмент | Версия | Зачем |
|---|---|---|
| **Unity** | **6000.4.1f1** | Основной движок |
| **Git LFS** | последняя | Хранение бинарных файлов (арт, звук) |
| **Python** | 3.10+ | Нужен для MCP сервера |
| **uv / uvx** | последняя | Запуск MCP сервера (`pip install uv`) |

### Клонирование и начало работы

```bash
# 1. Клонировать репозиторий
git clone https://github.com/1Nikolass/GameJam.git
cd GameJam

# 2. Инициализировать Git LFS (один раз на машине)
git lfs install

# 3. Создать свою рабочую ветку и переключиться на неё
git checkout -b dev-ИМЯ      # например: dev-danya или dev-nil

# 4. Открыть проект в Unity 6000.4.1f1
```

### Рабочие ветки

| Участник | Ветка |
|---|---|
| Nikolass | `dev-nikolas` |
| Danya | `dev-danya` |
| Nil | `dev-nil` |

---

## Правила командной работы

### Обязательно для всех

> **Asset Serialization Mode = Force Text** должен быть включён у каждого участника.
> Это единственный способ нормально работать в команде через Git.
> Проверить: `Edit → Project Settings → Editor → Asset Serialization → Mode: Force Text`

### Ветки и коммиты

- Каждый работает в **своей ветке** (`dev-nikolas`, `dev-danya`, `dev-nil`)
- Мержимся в `main` **часто и маленькими кусками** — не копите большие изменения
- Перед мержем делайте `git pull origin main` и разрешайте конфликты у себя
- Формат коммита: `feat: добавил систему управления` / `fix: починил прыжок`

### Общие сцены — важно!

> **Перед правкой любого `.unity`-файла — предупреди команду в чате.**
> Одновременное редактирование сцены двумя людьми = гарантированный merge-конфликт.

### Workflow при мерже в main

```bash
git checkout main
git pull origin main
git checkout dev-ИМЯ
git merge main              # подтянуть свежие изменения из main
# разрешить конфликты если есть
git checkout main
git merge dev-ИМЯ
git push origin main
```

---

## MCP for Unity (CoplayDev)

Пакет `com.coplaydev.unity-mcp` уже добавлен в `Packages/manifest.json`.

Для использования MCP сервера нужен **Python + uv**:

```bash
# Установить uv (если ещё нет)
pip install uv
# или через curl: curl -LsSf https://astral.sh/uv/install.sh | sh

# Запустить MCP сервер
uvx --from mcpforunityserver==9.7.3 mcp-for-unity
```

После установки пакета в Unity откройте `Window → MCP For Unity → Server Window` и запустите сервер.

---

## Структура проекта

```
Assets/
└── _Project/
    ├── Scripts/    — весь C# код
    ├── Scenes/     — сцены
    ├── Prefabs/    — префабы
    ├── Art/        — спрайты, текстуры, модели
    ├── Audio/      — звуки и музыка
    └── UI/         — UI-элементы
```

---

## Git LFS — что хранится в LFS

Следующие типы файлов автоматически уходят в LFS (см. `.gitattributes`):

`*.png *.jpg *.psd *.fbx *.wav *.mp3 *.ogg *.exr *.tga *.mp4`
